datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Referral {
  refereeEmail  String @id
  refereeName   String 
  referrerName  String
  referrerEmail String
}
