datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Referral {
  refereeEmail  String 
  refereeName   String @id
  referrerName  String
  referrerEmail String
}
